rootProject.name = "Chunkis"

includeBuild("core") {
    dependencySubstitution {
        substitute module("io.liparakis.chunkis:chunkis-core") using project(":")
    }
}
includeBuild("cismigrator") {
    dependencySubstitution {
        substitute module("io.liparakis.chunkis:chunkis-cismigrator") using project(":")
    }
}
includeBuild("fabric")
